const DATA_ATTR_NAME="data-checked";class Radio{constructor(t){const e=`input[name="${t}"]`;this.inputElements=Array.from(document.querySelectorAll(e)),this._updateCheckedAttributes(),this.inputElements.forEach(t=>{t.addEventListener("change",()=>{this._updateCheckedAttributes()})})}_updateCheckedAttributes(){this.inputElements.forEach(t=>{t.parentElement.setAttribute("data-checked",t.checked)})}}const DATA_ATTR_SELECTOR="[data-parent-check]";function initRadios(){return Array.from(document.querySelectorAll(DATA_ATTR_SELECTOR)).map(t=>new Radio(t.name))}export default initRadios;